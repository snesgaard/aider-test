[project]
name = "aider-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    
]
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "numpy",
    "ruff",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
